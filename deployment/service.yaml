apiVersion: v1
kind: Service
metadata:
  name: quickbite-sv-lg
spec:
  type: NodePort
  selector:
      app: quickbite
      env: dev
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
    nodePort: 30003